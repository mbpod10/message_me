<%= link_to 'Chatroom', root_path, class: "item"%>
<%= link_to 'Users', users_path, class: "item"%>
<a class="item"> Messages </a>
<div class="right menu">

  <% if logged_in? %>
    <div class="ui dropdown item"><%= current_user.username %><i class="dropdown icon"></i><div class="menu">
  <% else %>
    <div class="ui dropdown item">Accounts<i class="dropdown icon"></i><div class="menu">
  <% end %>

      <% if logged_in? %>
        <%= link_to 'Log Out', logout_path, method: :delete, class: "item"%>
      <% else %>
        <%= link_to 'Log In', login_path, class: "item"%>
      <% end %>

    </div>
  </div>

  <% if !logged_in? %>
    <div class="item">      
      <%= link_to "Sign Up", signup_path, class: "ui primary button"%>
    </div>  
  <% end %>
</div>