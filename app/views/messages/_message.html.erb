<div class="event active" id="message_<%= message.id %>">
  <div class="content">
    <div class="summary">
      <div class="flex item">

        <div>
          (<%= message.created_at.strftime("%m/%d/%y %I:%M%p") %>) 
          <a><%= link_to message.user.username, user_path(message.user) %>: </a> <%= message.body %>
        </div>

        <div>
          <% if current_user.id == message.user_id%>            
             <%= link_to "/message/#{message.id}", :method => 'delete' do %>
               <i class="trash alternate icon red"></i>
             <% end %>          
          <% end %>
        </div>

      </div>
    </div>
  </div>
</div>